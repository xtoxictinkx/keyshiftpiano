{
  "name": "key-shift-piano",
  "version": "0.1.6",
  "description": "A local desktop app for reading and transposing MusicXML and PDF sheet music.",
  "main": "src/main/main.js",
  "author": "Key Shift Piano",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "electron .",
    "test:js": "node --test tests/*.test.js",
    "test:python": "python -m unittest discover -s tests",
    "build:engine": "node scripts/build-engine.js",
    "pack": "npm run build:engine && electron-builder --dir",
    "dist:win": "npm run build:engine && electron-builder --win nsis"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.keyshiftpiano.app",
    "productName": "Key Shift Piano",
    "electronDist": "node_modules/electron/dist",
    "npmRebuild": false,
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "python/dist/transposer.exe",
        "to": "python/transposer.exe"
      }
    ],
    "win": {
      "target": "nsis",
      "signAndEditExecutable": false
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "allowElevation": false,
      "allowToChangeInstallationDirectory": false,
      "packElevateHelper": false,
      "include": "scripts/installer.nsh",
      "createDesktopShortcut": "always",
      "createStartMenuShortcut": true,
      "shortcutName": "Key Shift Piano"
    }
  }
}
